# Нижние границы, а не жёсткие пины: на свежих версиях Python (3.13, 3.14)
# нужны свежие сборки библиотек, иначе pip пытается компилировать их из исходников.
aiogram>=3.15,<4
aiosqlite>=0.20,<1
httpx>=0.27,<1
cryptography>=43
python-dotenv>=1,<2
# Нужен aiogram, когда Telegram открывается только через прокси.
aiohttp-socks>=0.9
